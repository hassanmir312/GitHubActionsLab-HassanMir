# GitHubActionsLab-HassanMir

Purpose of Dependent Jobs Workflow:

This workflow demonstrates job dependencies using the "needs" keyword. Jobs execute in sequence: build → test → deploy.

Purpose of Multi-Platform Workflow:

This workflow demonstrates parallel execution on multiple operating systems (Ubuntu, Windows, macOS).

Key Concepts Demonstrated

needs (job dependency)

runs-on (runner OS)

pull_request trigger

push trigger

Parallel vs Sequential execution

challenge:
n/a
